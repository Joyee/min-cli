{
  "name": "mini-cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "bin": {
    "mvc": "./bin/mvc.ts"
  },
  "scripts": {
    "prepare": "husky install",
    "lint": "prettier --check",
    "format": "prettier --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^10.0.0"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "inquirer": "^9.1.4",
    "lint-staged": "^13.1.2",
    "prettier": "^2.8.4"
  },
  "lint-staged": {
    "*.{js,ts,vue,json}": [
      "prettier --write"
    ]
  }
}
